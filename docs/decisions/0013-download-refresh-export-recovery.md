# ADR-0013：下载令牌刷新、任务恢复与公共目录导出

- 状态：已接受，失败任务自动换令牌重试仍待扩展
- 日期：2026-07-24

## 背景

视频 MP4 URL 带有时效令牌。用户从详情页选择清晰度到后台任务真正开始之间可能存在延迟，直接保存旧 URL 会增加失败概率。App 私有目录也不便于用户在文件管理器中长期保存视频。

## 决策

- 入队前重新请求视频详情，以清晰度标签选择最新 URL；
- 刷新后目标清晰度消失时停止入队并要求用户重新选择，不静默降级；
- 重复下载检查先于网络刷新，避免无意义请求；
- 后台下载器启用任务跟踪，并在初始化时调用 `rescheduleKilledTasks` 恢复数据库中被系统杀掉的任务；
- 已完成任务先在 App 私有目录创建临时副本，再通过 `moveFileToSharedStorage(..., SharedStorage.downloads)` 将临时副本导出到公共 `Downloads/Flule34`；
- 原始私有下载和账号记录不会因导出而删除；导出成功后可从提示条交给外部播放器打开，已导出的公共副本由用户自行管理。

原生 `onTaskStart` 回调只能决定任务是否提前结束，不能安全调用带账号 Cookie 的站点 API 并替换任务 URL，因此不把它描述为令牌自动刷新方案。

## 验证

- Repository 测试继续验证账号归属、入队、进度、取消、删除和本地数据清理；
- 新增公共目录导出测试；
- 下载平台与详情页测试替身同步实现导出接口；
- Flutter 静态分析和针对性测试通过。

## 后续

1. 为失败记录保存视频 slug，并实现用户触发的“刷新令牌并重试”；
2. 根据 HTTP 401/403/404 区分令牌失效与永久缺失；
3. 真机验证 Android 10–16 的 MediaStore 导出、权限和重复文件名行为；
4. 增加存储空间统计、并发数和保留策略。
