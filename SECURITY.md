# 安全政策

## 支持范围

首个公开版本发布前仅维护 `main`。公开发布后，最新稳定版本接受安全修复；旧版本可能要求先升级后再验证问题。

## 报告安全问题

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能。不要在公开 Issue 中披露：

- 可复现的账号接管、会话泄露或 Cookie 泄露；
- 签名私钥、仓库 Secret 或真实用户凭据；
- 任意文件访问、路径穿越、危险 Intent 或不安全 APK 更新链路；
- 能绕过账号数据隔离、年龄确认或访问控制的问题。

报告应包含受影响版本、Android 版本、最小复现步骤、影响与建议修复方式。诊断信息必须先自行检查；Flule34 的内置诊断不会包含 Cookie、密码、邮箱、用户 ID 值或设备标识符。

维护者会尽快确认收到报告，在修复和发布前与报告者协调披露时间。请不要访问、修改或下载不属于你的数据。

## 供应链

- Release 只能由固定签名密钥签署；
- GitHub Actions 使用固定提交 SHA 的官方 Action；
- Release 提供 SHA256，公开仓库同时生成 GitHub artifact attestation；
- App 不静默下载或安装 APK，只打开构建时配置的官方发布页。
- Android 清单显式禁止明文网络流量，并排除系统云备份与设备迁移中的 App 私有数据。
