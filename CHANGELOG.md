# 更新记录

本项目遵循语义化版本。发布日期以 GitHub Release 为准。

## [Unreleased]

## [1.4.4] - 2026-07-30

### Changed

- 播放进度改为设备级本地数据，未登录、切换账号或退出账号均可继续恢复；
- 关闭“记忆播放进度”时会确认并彻底清除全部本地进度；
- 下载启动阶段显示“正在连接”，收到有效数据后再显示体积和真实进度；
- 后台下载与播放通知改为低打扰通用文案，不再暴露标题、作者、文件名、清晰度或封面；
- 下载任务通知合并显示，完成、失败、暂停和取消不再产生额外通知。

### Fixed

- 修复续播跳转未实际生效时，画面从头播放但进度条停在旧位置的问题；
- 修复点击或滑动进度条、切换清晰度时可能出现的画面与进度状态分离；
- 修复下载开始前长时间停留在“正在下载”以及首个数百字节分片造成的误导；
- 修复置顶按钮可能遮挡筛选菜单、底部弹窗和对话框的问题；
- 缩短下载连接超时，并增强系统或网络中断后的自动恢复。

## [1.3.0] - 2026-07-27

### Added

- 新增账号播放列表，支持创建、重命名、分页浏览、批量管理视频归属和沉浸式连续播放；
- 播放列表支持顺序播放、列表循环和单集循环，并提前预缓存下一视频；
- 首页新增可选双列布局，媒体库的收藏、历史和本地分类库新增搜索与筛选；
- 下载管理新增仅删除记录、清理失效记录以及批量删除记录和文件；
- 视频卡片、本地分类库和播放列表统一显示浏览量、点赞率及对应图标。

### Changed

- 全屏播放器进入时默认隐藏控件，退出后立即显示普通播放器控件，并修复进入前控件跳变及退出时控件提前变形；
- 恢复历史播放位置时不再显示额外提示；
- 清晰度和倍速菜单跟随 App 主题配色，全屏控制栏增加左右操作空间；
- 视频卡片将“入库”和“加入播放列表”统一命名为“本地分类库”和“播放列表”，移出操作收纳到三点菜单；
- 首页关注内容严格按发布时间排序；
- 默认播放清晰度改为 1080P，不存在对应清晰度时自动回退到较低的最高清晰度；
- 播放列表切换视频时继承全屏状态，并采用更积极的下一集预缓存策略；
- 精简发现、媒体库和设置页面的标题、入口与说明文字；
- 项目开源协议由 Apache License 2.0 改为 MIT License。

### Removed

- 移除隐藏标题关键词设置；
- 移除本地调试日志功能，并在首次启动时清理旧日志和相关偏好；
- 移除发现页无实际内容的频道入口及仓库中的社区规范和 Issue 模板。

### Fixed

- 修复部分艺术家和上传者在媒体库订阅页缺少头像，以及订阅列表只能加载第一页的问题；
- 修复本地分类库视频被错误标记为网站收藏的问题；
- 修复手动检查更新无法识别 GitHub Release APK 的问题；
- 修复部分视频网页提供 2160P、App 却只能识别到 1080P 的问题；
- 修复播放列表名称混入视频数量文本、视频无法移出列表以及连续播放退出全屏的问题；
- 修复下载文件删除失败、外部文件失效状态和批量清理行为；
- 修复保持设备当前方向时全屏控制层缩在画面中央的问题；
- 修复双列首页卡片留白过多和发布时间缺失的问题；
- 修复登录状态下卡片菜单因等待收藏状态而延迟弹出的问题。

## [1.2.0] - 2026-07-27

### Added

- 新增与账号无关的本地分类库，支持创建、重命名、删除、入库和移出；
- 内置 HydraFXX、Nagoonimation、Bamh3D、JuicyNeko 与 Drills3D 五个作者精选库，共 289 个经本地收藏、版本和质量条件筛选的视频；
- 精选库采用一次性幂等导入，兼容同名用户库，用户删除后不会自动恢复；
- 登录账号和密码可保存在 Android 安全存储中，会话失效后自动重新登录；
- 视频列表优先加载详情级高清封面，失败时回退到网站缩略图。
- 新增可选的本地调试日志，可保留 1～7 天并查看、清除或分享脱敏日志。
- 新增统一精筛面板，支持标签、艺术家、分类严格交集、排除条件、最低点赞率和最低投票数；
- 新增上传者主页、公开视频列表与上传者订阅；
- 新增本地播放器性能日志，记录详情加载、播放器准备和缓冲耗时；

### Changed

- 下载管理与下载设置合并为“我的”中的统一入口；
- Release 仅构建并发布 Android arm64 APK；
- 视频评分、票数和元数据关联票数保留只读展示，不再允许用户投票；
- 播放器控制栏统一高度，并为横屏全屏按钮增加右侧操作空间。
- 横屏全屏控制层新增左上角退出按钮和视频标题；
- 下载固定保存到公共 `Download/Flule34`，下载卡片改为封面、清晰度、进度、体积与有效状态布局；
- 搜索自动补全和热门标签统一进入对应实体集合页，集合页使用统一精筛；
- “我的”移除低价值说明文字，并精简 App 设置与帮助反馈入口。
- 首页移除占用空间的品牌标题和盾牌行，搜索栏直接置顶；
- 重构应用图标的矢量几何，使 F 与播放三角保持清晰间距并统一各密度资源；
- 账号、上传者和艺术家统一使用可回退的网络头像组件。
- 媒体库订阅按可见项目补全艺术家和上传者头像，并缓存重复请求；
- 视频详情与播放源增加短时内存缓存和并发请求合并，失败、重试及下载前强制刷新；
- 播放器启动前并行读取网络状态和历史进度，并在加载期间显示视频封面；
- “我的”放大个人信息卡片并增加与功能入口之间的留白；

### Removed

- 移除播放列表、稍后观看、喜欢/不喜欢和元数据投票功能；
- 移除冷启动自动检查更新，保留手动检查更新。
- 移除视频卡片长按预览和相关设置；
- 移除用户选择下载目录的功能。

### Fixed

- 修复从相关视频进入下一视频后两个播放器同时播放；
- 修复首次下载目录授权链路、下载错误登录依赖和 URI 下载暂停能力错配；
- 修复 URI 下载缺少 AndroidX DocumentFile 而导致的原生闪退，并在保存和使用目录前校验持久读写权限；
- 修复新建本地分类库成功后弹窗退出阶段触发 Flutter 生命周期断言；
- 修复“我的”页面返回动画底色变化和二级页面卡片粘连。
- 修复视频卡片快捷菜单无法识别已收藏状态；
- 修复普通 HTTP 403 被误判为登录过期并清除会话；
- 修复从搜索建议进入集合后未显示结果、筛选按钮异常出现红点；
- 修复已完成下载体积显示为 0B，并在外部文件删除、改名或体积变化后标记为失效。
- 修复 Android 15 MediaStore 文件被 `DocumentFile` 误判为不可读，导致成功下载显示失败；
- 修复关键词搜索和空关键词精筛从第二页起使用错误 URL、提前显示“已经到底了”；
- 修复艺术家 URL slug 与数值筛选 ID 混用，导致部分集合页没有正确限定艺术家；
- 修复调试报告仍按账号统计设备级下载、始终显示 0 条的问题。
- 修复媒体库订阅页艺术家和上传者头像始终为空；
- 修复订阅页下拉刷新仍复用旧缓存；
- 修复视频详情和播放器重复请求相同视频源；

## [1.1.1] - 2026-07-25

### Changed

- 媒体库精简为收藏、历史和订阅三个页签；
- 播放器控制层改为时间独占上行，播放、进度条、清晰度、速率和全屏严格位于同一底行；
- 发现目录搜索改为全站服务端搜索，并增加防抖、竞态保护和明确状态提示。

### Fixed

- 修复收藏等分页列表到达末尾后把 HTTP 404 显示为加载失败；
- 修复播放器总时长被 52 像素播放按钮区域裁掉；
- 修复标签与分类搜索使用错误参数而返回热门项；
- 修复标签目录第二页误进入 ID 为 2 的 `pov` 标签页；
- 修复频道目录在空列表后继续请求不存在的第二页。

## [1.1.0] - 2026-07-25

### Added

- 使用 Better Player Plus 重构详情页播放器，加入持久缓存、后台媒体通知和可配置后台播放；
- 新增单行透明播放器控制栏，清晰度与倍速使用独立按钮；
- 视频详情页播放器固定在顶部，下方信息可独立滚动；
- 下载直接写入用户授权的公共目录，默认显示为 `Downloads/Flule34`；
- “我的”新增下载管理入口，支持暂停、继续、重试、播放和删除；
- 每次冷启动自动检查 GitHub Release，新版本可直接打开对应 APK；
- 新增浅色、中性深色和跟随系统主题，统一 Material 3 表面层级与间距。

### Changed

- “最高可用”不再被移动网络自动策略强制降为 480p；
- 视频卡片统一为单列布局，封面仅显示时长，标题下展示发布时间、点赞率/票数和浏览量；
- 下载改为设备级能力，不再要求登录或按账号隔离；
- 媒体库移除下载页签，下载任务集中到“我的”；
- 帮助、隐私与设置文案同步新的下载和播放行为。

### Removed

- 移除首次启动成年确认；
- 移除评论展示、发表评论、评论投票、解析模型及相关测试；
- 移除 App 私有下载副本和“导出到 Downloads”流程；
- 移除快退、快进和静音控制按钮，双击画面仍可快退或快进 10 秒。

### Fixed

- 修复首次成年确认后界面可能卡住的问题（功能现已整体移除）；
- 修复详情播放器随页面滚动销毁、缓存丢失和重新黑屏加载的问题；
- 修复全屏视频比例失真、控制层过高且背景遮挡画面的问题；
- 修复切换应用、小窗或锁屏时无条件停止播放的问题；
- 修复深色主题偏黑紫、三个主题选项观感接近的问题；
- 修复视频卡片标题重复、时长缺失和统计信息不完整的问题。

## [1.0.0] - 2026-07-25

- 首个验证性 Android 版本，建立四栏导航、账号会话、媒体库、搜索、发现、视频详情、下载与发布基础设施。
